# Enforce the `engines` floor in package.json (Node >= 24): without this npm
# only warns on an incompatible runtime. CI and the Docker build use Node 26,
# so the guard exists to fail fast on stale local toolchains.
engine-strict=true
