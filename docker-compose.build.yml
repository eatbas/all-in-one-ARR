# Developer override: build the aio-arr image from source instead of pulling it.
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up --build
services:
  aio-arr:
    build: .
